# Agregar al final de create_app() en app/__init__.py

# Inicializar gestor de PDFs
from app.pdf_manager import PDFManager
pdf_manager = PDFManager(app.config.get('UPLOAD_FOLDER', 'app/uploads/resultados'))

# Hacer disponible globalmente
from flask import g
@app.before_request
def before_request():
    g.pdf_manager = pdf_manager
