# ============================================
# CONFIGURACIÓN PARA RENDER
# ============================================
# Copia este archivo y configura estas variables en:
# Render Dashboard → Tu Servicio → Environment

# Base de Datos (OBLIGATORIO)
# Obtén esto desde Supabase Dashboard → Settings → Database → Connection String
# IMPORTANTE: Usa la URI de "Connection pooling" para mejor rendimiento
DATABASE_URL=postgresql://postgres.[TU_PROYECTO]:[TU_PASSWORD]@aws-1-us-east-2.pooler.supabase.com:5432/postgres

# Secret Key para Flask (OBLIGATORIO)
# Genera uno random o usa: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu_secret_key_aqui_cambiar_esto

# Supabase (OPCIONAL - solo si usas funciones de Supabase directamente)
SUPABASE_URL=https://[TU_PROYECTO].supabase.co
SUPABASE_KEY=tu_supabase_anon_key_aqui

# ============================================
# NOTAS IMPORTANTES PARA RENDER:
# ============================================
# 1. NO uses localhost en DATABASE_URL
# 2. Usa el pooler de Supabase (pooler.supabase.com) NO el directo
# 3. El puerto DEBE ser 5432
# 4. Asegúrate de que Supabase permita conexiones externas
# 5. Render detecta automáticamente Python y requirements.txt
